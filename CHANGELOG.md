# 变更日志

## v1.0.6（2026-08）

### 新增
- **图表缩放（替代滚轮横滑）**：滚轮以**鼠标位置为锚点放大/缩小**（`transition` 动效）；
  每张图表右上角新增 **− ＋ ↺** 缩放控件（放大/缩小/复位，1.3 倍步进）；
  放大后图表超出容器，可**拖拽平移**（拖动不触发数值钉住）。
- **README 添加实例截图**：中文/英文报纸报告、Web 界面预览图
  （`examples/screenshots/`，README.md 与 README.en.md 均已嵌入）。
- **DEVELOPMENT.md 改为仅本地**：只保留在工作目录，不再同步 canonical/GitHub
  （v1.0.5 误传，v1.0.6 起从仓库移除）。
- 版本号 1.0.6，Git 标签 1.0.6。

### 变更
- 滚轮在图表上不再左右滚动，改为缩放；平移通过拖拽或滚动条完成。

## v1.0.5（2026-08）

### 新增
- **图表滚轮横滑**：柱状/堆叠柱数据点 >14、折线 >31 时，图表自动加宽（固定像素宽），
  容器内滚轮滚动即水平滑动，带细滚动条样式；数据点越多越美观。
- **点击钉住数值提示**：自绘提示气泡（报纸风），鼠标悬停查看；**点击柱/段/点/扇区/图例/条形
  即可钉住**，鼠标移走提示仍在（跟随鼠标），再点一次 / 点空白 / Esc 取消。
- 新增 **DEVELOPMENT.md** 内部开发接续文档：架构地图、数据流、i18n 约定、report 图表架构、
  发布流程（含 tag 规则）、已知问题与 TODO，供后续维护/开发会话使用。
- 版本号 1.0.5，Git 标签 1.0.5。

### 修复
- 报告内联脚本后旧测试「无 script」断言更新为「无外部 script」（内联脚本仍自包含）。

## v1.0.4（2026-08）

### 新增
- **多语言（zh / en）**：新增 `dsusage/i18n.py`；CLI 输出、报纸风 HTML 报告、Web 界面
  支持中英文切换。
  - CLI：`dsu --lang en ...`；自动检测顺序 `--lang` > 配置文件 `lang` > 环境变量
    `DSU_LANG` > 系统 locale。
  - HTML 报告：按生成时语言输出（标题/头版数据/图表标题/图例/表头/页脚）。
  - Web 界面：报头右上角 **EN / 中文** 切换按钮（localStorage 记忆）。
  - 新增 `README.en.md` 英文文档。
- **修复 Web 服务控制台报错刷屏**：`/favicon.ico` 404 被全局异常处理器二次抛出导致
  500 + 堆栈刷屏 —— 现在返回 204；非 API 路由的 404/错误不再打印堆栈。
- 版本号 1.0.4，Git 标签 1.0.4。

### 说明
- 导出的 Excel/CSV 表头保持中文（数据层产物）；HTML 报告与 Web 预览会随语言翻译。

## v1.0.3（2026-08）

### 新增
- **完整数字显示**：头版数据不再缩写（Token 合计 1.3B → 完整显示 1,300,000,000），
  单行不折行；图表悬停提示（`<title>`）同样给出完整数值。
- **图表悬停提示**：柱状图、堆叠柱（含缓存命中/未命中/输出拆分）、折线点（带透明大命中区）、
  环形图扇区与图例、横向条形图 —— 鼠标悬停即显示该项完整数据。
- **动效**：报头"压印"入场、区块淡入上移（交错延迟）、柱子自底生长、条形自左生长、
  折线描边绘制、面积淡入、悬停高亮（柱/扇区变淡、图表边框红框提示）。
- 版本号 1.0.3，Git 标签 1.0.3（tag 约定生效）。

### 修复
- 头版超长数字折行/断字（改单行 + 自适应字号 + 更宽卡片网格）。

## v1.0.2（2026-08）

### 新增
- **报纸编辑风 HTML 图表报告**：导出新增 `report.html` —— 报头/头版数据/内联 SVG 图表
  （每日费用柱状图、每日 Token 堆叠柱、小时走势折线、模型占比环形图、API Key 排名条形图）+
  数据表 + 印刷页脚；自包含无外链，双击即看。
- **一键导出 `dsu go`**：`dsu go --start X --end Y` = 全部格式（xlsx+csv+html）+ 官方原始数据 +
  自动用浏览器打开 HTML 报告。
- `day` / `range` 默认格式改为 `all`（xlsx + csv + html）；新增 `--open` 参数自动打开报告。
- Web 界面整体重排为报纸编辑风（衬线字体、双线报头、黑/米白/铅红配色），导出格式增加
  HTML 图表报告选项。
- 修复：平台 cost/amount 响应 `api_key` 为对象结构时的崩溃（归一化为稳定 id），
  新增 `dsu diagnose` 命令抓取原始响应核对结构。
- Windows 双击启动脚本：`启动Web界面.bat` / `一键导出.bat`。

### 修复
- 报告图表若干排版问题：柱/线超出绘图区上界、标题与刻度重叠、环形图中心文字对比度、
  图例与 X 轴标签重叠、边缘标签被裁切（视觉回归已验证）。

## v1.0.1（2026-07）

首版发布。

### 新增
- **登录态复用**：`dsu login / check / keys / logout`；Token 保存于 `~/.dsusage/config.json`。
- **小时级用量**：`--granularity hourly` 逐日请求强制小时桶，跨天合并为连续小时明细
  （平台后台仅单日可见小时，本工具任意范围均可）。
- **超长周期导出**：任意日期范围自动按 ≤30 天窗口分片抓取并去重合并。
- **官方原始导出**：调用平台 `usage/export` 下载 ZIP，保留 `amount-*.csv` / `cost-*.csv`
  原始文件（分片自动合并同名 CSV）。
- **多维汇总**：小时/每日明细、每日汇总、模型汇总、API Key 汇总、费用明细（多币种）。
- **导出格式**：Excel 多工作表（样式/冻结/筛选）、CSV（utf-8-sig）、meta.json。
- **API Key 筛选**：`--api-key <trackingId>` 支持逗号分隔多个。
- **Web 界面**：`dsu serve` 本地单页（Token 验证、范围/粒度选择、预览、后台导出任务、历史记录）。
- **容错**：429/5xx 指数退避重试；Token 失效明确报错；非 ZIP 响应检测。

### 修复 / 说明
- 平台导出接口偶发以 HTTP 200 返回 JSON 错误页：校验 ZIP 魔数（PK）后报错。
- 官方 CSV 表头随版本变化：按表头自适应解析，兼容 `YYYYMMDD` / `YYYY-MM-DD` / 带时间格式。

### 限制（已知）
- `hourly` 粒度逐日请求，N 天约需 2N 次接口调用，长周期请预留时间并注意频率。
- 费用列按币种合计展示；分币种明细见「费用明细」表。
